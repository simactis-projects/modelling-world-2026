# Quarto render output (regenerated by `quarto render`).
/.quarto/
# Rendered HTML and its supporting files are tracked for GitHub Pages, so the
# broad excludes below are disabled. See the explicit inclusions underneath.
# *.html
# /*_files/

# Track the rendered presentation output for GitHub Pages.
!modelling-world-2026.html
!modelling-world-2026_files/
